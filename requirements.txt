# Runtime
requests==2.34.2       # OpenAI-compatible HTTP client for the local LLM endpoint
mutagen==1.48.1        # tag + cover-art reading; 5.2x faster than shelling out to
                       # ffprobe, and reads the freeform atoms a generic reader drops

# Web UI (webapp/) — the CLI does not need it
flask==3.1.3
gunicorn==23.0.0       # production WSGI server; see gunicorn.conf.py for why it
                       # runs one worker with threads and not the other way round

# Development
pytest==8.4.2          # regression tests for the ontology, identity and scoring
